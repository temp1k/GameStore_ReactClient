import React from 'react';
import {Container} from "react-bootstrap";

const Profile = () => {
    return (
        <Container>
            Здесь будет профиль
        </Container>
    );
};

export default Profile;